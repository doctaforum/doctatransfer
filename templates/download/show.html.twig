{% extends 'base.html.twig' %}

{% block title %}Download{% endblock %}

{% block body %}
    <h1>Download</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ download.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ download.date ? download.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('download_index') }}">back to list</a>

    <a href="{{ path('download_edit', {'id': download.id}) }}">edit</a>

    {{ include('download/_delete_form.html.twig') }}
{% endblock %}
