<form method="post" action="{{ path('download_delete', {'id': download.id}) }}" onsubmit="return confirm('¿Estas seguro que deseas eliminar el registro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ download.id) }}">
    <button class="btn">Delete</button>
</form>
